# FIX — "An agent is writing a reply…" 무한 대기 + 상품 링크 소실 (2026-08-06)

## 1. 증상

1. 위젯에 `십대 남자용 스킨케어 제품 추천해줘` 입력 → **"An agent is writing a reply…" 상태로 멈춤**
   (답변 영영 오지 않음).
2. (기확인) AI 상품추천의 상품페이지 링크가 답변 직후 ~5초만 보이고 사라짐.

## 2. 근본 원인 (스테이징 로그·DB 증거)

대화 82번 실제 타임라인 (`messages`, `conversations` 조회):

| 시각 | 메시지 | 처리 |
|---|---|---|
| 07:04:36 | user "please recommend me, my skin is oily" | intent `product_recommendation` 0.95 → **AI 정상 답변 + 상품 URL 인용**(`ivyusa.com/products/...`) |
| 07:06:26 | user "thanks, and recomend my young son." | intent 0.78이나 **RAG 검색 신뢰도 < 0.45** → 자동 핸드오프(`low_confidence`) → `status=waiting`, `escalated=1` |
| 07:09:19 | user "십대 남자용 스킨케어 제품 추천해줘" | **봇이 의도적으로 침묵**(FR-S4: waiting/agent 상태에선 무응답) — `intent`/`retrieval_trace` 모두 NULL = **처리 자체를 안 함** |

즉 멈춘 원인은 이 질문의 실패가 아니라 **직전 메시지에서 이미 상담사 대기 상태로 전환**된 것.
질문 자체는 처리 대상에서 제외됐다.

추가 확인:
- `agent_profiles` **0건** — 스테이징에 상담사가 한 명도 등록돼 있지 않음. 미처리 알림 2건.
  → 대기열에 들어가면 **아무도 받지 않아 영구 대기**.
- 위젯(PR #107)이 `waiting`을 "담당자가 답변을 작성 중"으로 표시 → 아무도 작성하지 않는 상태를
  곧 답이 올 것처럼 보이게 함(표시 결함).
- 에스컬레이션을 유발한 "thanks, and recomend my young son."은 **맥락 의존 후속 질문** —
  RAG는 직전 대화를 참조하지 않고 이 문장만으로 검색하므로 유사도가 바닥.

## 3. 이번 수정 (배포됨)

1. **대기 상태 표시 정정** (`TypingBubble`, `ChatTab`):
   - `sending`(AI 요청 중) → 점 애니메이션 "답변을 작성 중입니다…"
   - `status=agent`(상담사가 수락함) → 점 애니메이션 "담당자가 답변을 작성 중입니다…"
   - `status=waiting`(대기열, 담당자 미배정) → **정적 시계 아이콘 + "상담사 연결을 기다리는 중입니다.
     답변이 등록되면 여기에 표시됩니다."** — 애니메이션/오해 제거 (en/es/ko)
2. **상품 링크(citations) 영속화**: `ChatMessageResponse.citations` 추가,
   `ChatMapper`가 저장된 turn의 `retrieval_trace.citations`를 함께 반환.
   위젯 `reconcile`도 인용을 보존. → 폴링 후에도, 새로고침 후에도 🛍 상품 링크 유지.

## 4. 개선안 처리 결과 (2026-08-06 승인 → A1·A2 구현)

| # | 제안 | 상태 |
|---|---|---|
| A1 | **대기 중에도 AI가 계속 답변** — 상담사가 수락하기 전까지 봇이 답하고, 수락(`agent` 또는 `agent_id` 배정) 후 침묵 | **구현 완료**. 이미 대기 중인 대화는 저신뢰/모더레이션 차단 시 **재에스컬레이션하지 않음**(중복 알림·중복 안내 방지, 이때만 무응답이며 위젯이 대기 상태를 계속 표시) |
| A2 | **RAG 후속 질문 맥락 반영** | **구현 완료**. 직전 고객 발화 2턴(각 200자)을 **검색 쿼리에만** 합성(모델에는 현재 질문만 전달), PII 스크럽 적용 |
| A3 | 운영: 상담사 배치 또는 오프아워 이메일 라우팅 | **미결 — 사용자 결정 필요**(§4-1) |
| A4 | 에스컬레이션 임계값 재검토 | **재분석 결과 무효**(§4-2). 대신 A2가 실질적 해결 |

### 4-1. A3 현황 (조치 필요 값)

- `tenant_ai_config.handoff_config` = **NULL**(테넌트 1·2 모두) → 라우터는 항상 "상담사에게 전달"
  모드로 동작하며, 오프아워 이메일 대체 경로가 없음.
- 테넌트 1에 콘솔 사용자 10명(모두 master, 콘솔에서 대화 수락은 가능)이 있으나
  `agent_profiles` **0건** — 상담 배정/가용성 관리 대상이 아무도 등록돼 있지 않음.
- 필요한 결정값: **업무시간·타임존·오프아워 회신 메일함**(선택: 담당자 지정 userIds).
  값을 주시면 콘솔/설정으로 반영 가능. A1 덕분에 그동안도 대화는 끊기지 않음.

### 4-2. A4 재분석 — 임계값 조정은 효과 없음

`RagService.confidence()`는 voyage 임베딩 경로에서 **이분적**이다:
최고 유사도 ≥ `RAG_MIN_SIMILARITY`(스테이징 0.45) → `clamp(best, 0.5..0.95)`, 미만이면 **0.2**.
따라서 `ESCALATION_CONFIDENCE`(0.45)를 0.2~0.5 사이 어디로 옮겨도 결과가 바뀌지 않는다.
실제 손잡이는 `RAG_MIN_SIMILARITY`이며, 이를 낮추면 근거 없는 답변(환각) 위험이 커지므로
계측 없이 조정하지 않았다. A2로 쿼리 품질이 올라가면 유사도 자체가 올라간다.

## 5. 예방 패턴

**"대기/진행" UI는 실제로 누군가 작업 중일 때만 진행형으로 표기**한다. 큐에 들어간 상태를
진행형으로 표시하면 사용자는 무한정 기다린다 — 상태 표시는 시스템의 실제 상태(배정 여부)와
1:1로 대응시킬 것. 또한 **응답에만 실리고 저장 응답에는 없는 필드**(citations 사례)는 폴링
동기화 순간 사라진다 — 서버가 이미 저장하고 있다면 조회 응답에도 반드시 포함시킨다.

## 6. 기록

- 1차(표시 정정 + citations 영속화): **PR #111 `8232844`**, 스테이징 배포 완료
- 2차(A1 대기 중 AI 응답 + A2 검색 맥락): **PR #112 `57e525c`**, 스테이징 배포 완료
- 관련: PR #107(타이핑 인디케이터 도입), PR #106(상품 링크 기능)
- 테스트: `chat.mapper.spec.ts` citations 3케이스 + `chat.service.queued.spec.ts` 7케이스,
  apps/api **51 suites / 533 tests PASS**, typecheck·build·실부팅 확인

## 7. 스테이징 실검증 (2026-08-06, 배포본 `57e525c`)

실제 위젯 API로 대화를 생성해 확인 (`/session/ensure` → `/session/consent` → `/chat/message` → `/chat/conversation`).

| 시나리오 | 결과 |
|---|---|
| **T1** 문제의 질문 `십대 남자용 스킨케어 제품 추천해줘` (ko) | **AI 정상 답변**(한국어), 인용 4건 전부 상품 링크. 예: `lagom-cellup-ph-cure-foam-cleanser`, `jmsolution-teatree-trouble-clear-toner-pads` |
| **T1-b** 대화 이력 재조회 | `citations=4 / product_links=4` — **폴링·새로고침 후에도 상품 링크 유지**(PR #111 검증) |
| **T2** 사고를 유발했던 후속 질문 `thanks, and recomend my young son.` | **에스컬레이션 없이 AI 답변**, `status=ai_active` 유지 → **A2 효과 확인**(종전 low_confidence 핸드오프) |
| **T3** 무의미 질문으로 에스컬레이션 유발 → `status=waiting` | 핸드오프 안내 정상 |
| **T3-b** 대기 상태에서 상품 질문 | **AI가 정상 답변**(상품 링크 4건), `status=waiting` 유지(상담사 대기 그대로) → **A1 효과 확인**(종전 무응답) |

### 7-1. 인용 관련성 — 조치 완료 (PR #114 `93a6657`, 2026-08-06)

**문제**: 인용 목록이 검색 상위 4건을 그대로 노출해, 답변이 언급하지 않은 상품 링크가 함께 붙었다
(예: 폼 클렌저를 추천한 답변에 컨실러·나이트크림 링크).

**해결 방식**: 답변 본문과 상품명 문자열 대조는 불가 — 답변은 현지어("라곰 셀업 pH 큐어 폼 클렌저")인데
카탈로그 제목은 영문(`LAGOM Cellup pH Cure Foam Cleanser`)이라 매칭이 전부 실패한다. 대신 컨텍스트 항목에
번호를 붙이고 모델이 마지막 줄에 내부용 `CITED: 1,3`을 남기게 한 뒤, **모더레이션·저장·노출 이전에 그
줄을 제거**하고 해당 항목만 인용으로 남긴다. 마커가 없으면 전체 인용 유지(fail-open) — 형식 미준수 시
링크가 하나도 없는 상태보다 종전 동작이 낫기 때문.

**스테이징 실검증** (`93a6657`):

| 케이스 | 답변이 언급한 상품 | 노출된 인용 | 마커 유출 |
|---|---|---|---|
| ko `십대 남자용 스킨케어 제품 추천해줘` | 라곰 pH 큐어 폼 클렌저 / JM솔루션 티트리 패드 / SNOW2+ AC 패치 | **정확히 그 3건**(전부 상품 링크) | 없음 |
| en `gentle cleanser for oily acne prone skin` | LAGOM pH Cure Foam / Dear Ohneul Mud Cleanser / JMsolution Toner Pads | **정확히 그 3건** | 없음 |
| en `what is your refund timeline?` | (정책 안내) | counsel 문서 1건, 상품 링크 없음 | 없음 |

종전 동일 질문에서 4건 중 2~3건이 답변과 무관했던 것 대비, 세 케이스 모두 **본문 언급 상품과 1:1 일치**.
