# TCR-PWA-20260802 — PWA + Web Push 파이프라인 테스트

- 작성일: 2026-08-02
- 대상: PLN-PWA-20260802 P1(백엔드 Web Push) + P2/P3(apps/pwa) + P4(스테이징 배포)
- 환경: 로컬 dev(API :3009 실부트) + 스테이징 shoptalk.amoeba.site

## 1. 단위 테스트 (Jest — `npm test --workspace=@ivy/api`)

`push.service.spec.ts` 12 케이스 (기존 8 갱신 + 신규 4):
| # | 케이스 | 결과 |
|---|---|---|
| U9 | webpush register: 구독 JSON → provider 'webpush' + 64-hex token_hash(SHA-256 일치) | PASS |
| U10 | webpush register: http 엔드포인트/keys 누락 → E5006 BusinessException | PASS |
| U11 | dispatch provider 라우팅: 동일 고객의 expo 행+webpush 행 → 각자의 프로바이더로만 발송 | PASS |
| U12 | webpush 410(shouldRevoke) 티켓 → token_hash 기준 revoke | PASS |
| U1-U8 | 기존 expo 등록/재바인딩/해지/디스패치 전부 회귀 통과 (upsert 기준이 token→token_hash로 이전) | PASS |

**전체 스위트: 28 suites / 273 tests ALL PASS** (269 → +4)

## 2. 통합/스모크 — 로컬 (실부트 후 HTTP)

| # | 시나리오 | 기대 | 결과 |
|---|---|---|---|
| S1 | 엔티티 변경 후 실부트 | successfully started (A-1) | PASS |
| S2 | `migration_push_webpush.sql` 로컬 적용 | token TEXT + token_hash UNI + 기존 expo 행 backfill(SHA2 일치) | PASS(수정 1회 후 — §4) |
| S3 | GET /push/vapid-key | publicKey(87자) | PASS |
| S4 | webpush 등록 — http 엔드포인트 | 400 E5006 | PASS |
| S5 | webpush 등록 — 정상 구독 | provider:webpush, active:true | PASS |
| S6 | **E2E**: 배송 웹훅 → 알림 → PUSH_DISPATCH → WebPushProvider → **실제 FCM Web Push 엔드포인트 호출** | 가짜 구독에 404/410 → 자동 revoke + `devices=1 sent=0` 로그, DB revoked=1 | PASS |
| S7 | expo 경로 회귀 (S6과 동일 고객의 expo 행) | provider별 분리 발송 (로그·mock DB 확인) | PASS |

## 3. PWA 빌드/정적 검증
| 항목 | 결과 |
|---|---|
| `tsc --noEmit` strict (apps/pwa) | PASS |
| `vite build` (base /app/) | PASS — JS gzip 92.2KB, sw.js/manifest/아이콘 dist 포함 |
| 모노레포 typecheck 9 tasks / build | PASS |
| i18n en/es/ko 73키 동일성(스크립트 대조) | PASS — 하드코딩 UI 문자열 없음 |
| 아이콘 PNG 유효성(file) | PASS (192/512) |

## 4. 발견·수정된 결함 (구현 중)
| # | 결함 | 수정 |
|---|---|---|
| D1 | migration 순서 오류 — 기존 UNIQUE(token) 유지 상태에서 `MODIFY token TEXT` → MySQL 1170 | 인덱스 이전(ADD uk_token_hash + DROP uk_token)을 TEXT 확장 **앞**으로 재배치, 로컬 재검증 |
| D2 | 배포 후 `/app/*`가 관리자 SPA로 폴백 — edge nginx 단일파일 bind-mount가 git pull(rename) 후 옛 inode 참조, reload 무효 | force-recreate로 즉시 복구 + deploy 스크립트 영구 수정. 상세: `docs/bug-fix/FIX-StagingNginxStaleMount-20260802.md` |

## 5. 스테이징 검증 (P4)

| # | 항목 | 결과 |
|---|---|---|
| G1 | migration 코드 배포 **전** 선적용 (`ivy_mysql_staging`) | PASS — token TEXT/token_hash UNI 확인 |
| G2 | `.env.staging`에 VAPID 키(스테이징 전용 신규 생성)+VITE_SHOP_DOMAIN 추가 | PASS |
| G3 | deploy-staging.sh — pwa 이미지 첫 빌드 포함 | PASS — ivy_pwa_staging Up, api healthy·successfully started |
| G4 | `/app/` 200(PWA index), manifest `application/manifest+json`, `sw.js` `application/javascript`+`no-cache` | PASS (D2 수정 후) |
| G5 | `GET /api/v1/push/vapid-key` 스테이징 공개키 | PASS (87자) |
| G6 | 스테이징 webpush 구독 등록/해지 | PASS — provider:webpush, revoked:true |

## 6. 미검증 항목 (후속)
| 항목 | 사유 | 계획 |
|---|---|---|
| 실브라우저 푸시 수신·클릭 딥링크 | 브라우저 Push API는 실사용자 제스처/설치 필요 — curl 불가 | Android Chrome + 설치형 iOS Safari에서 수동 스모크(설치→구독→배송 웹훅→수신) |
| iOS A2HS 설치 흐름 | 실기기 필요 | 위와 동일 |
| SW 오프라인 셸/업데이트 교체 | 실브라우저 필요 | 위와 동일 |
| 구독 만료 rotation | 장기 관찰 필요 (부트 시 멱등 재등록으로 커버) | 운영 관찰 |
