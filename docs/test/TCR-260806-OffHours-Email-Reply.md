# TCR — 오프아워 이메일 회신 + 운영시간 설정화 (2026-08-06)

> 근거: `docs/plan/PLN-260806-OffHours-Email-Reply.md` (승인: D1 권장안, D3 제안, 테스트 수신 dev@amoeba.group)

## 1. 단위 테스트 (apps/api)

| ID | 대상 | 케이스 | 결과 |
|---|---|---|---|
| U1 | `HandoffRouterService` | 휴게(12–13 NY) 중 문의 → `mode=email` | PASS |
| U2 | 〃 | 휴게 앞뒤(11:30/13:30) → `mode=agents` | PASS |
| U3 | 〃 | 형식오류·동일시각·빈 배열 휴게 → 근무시간 유지(닫지 않음) | PASS |
| U4 | `ChatService.saveContactEmail` | 저장 + 세션 바인딩 + 세션 캐시 무효화 + 확인 메시지 | PASS |
| U5 | 〃 | 동의 GRANTED 아니면 거부(수집 자체를 안 함) | PASS |
| U6 | `AgentService.sendMessage` | `reply_channel='email'` + 주소 보유 → 메일 발송 + 전송 알림 시스템 메시지 | PASS |
| U7 | 〃 | `reply_channel=null`(위젯 열람 중) → 미발송 | PASS |
| U8 | 〃 | 주소 미보유 → 조용히 건너뜀(상담 흐름 유지) | PASS |

전체: **53 suites / 546 tests PASS**, `typecheck`·`build` 통과, API 실부팅 확인(신규 컬럼).

## 2. 통합 시나리오 (스테이징)

| ID | 시나리오 | 기대 |
|---|---|---|
| S1 | 업무시간 외 문의(이메일 미보유) | 안내 "회신받으실 이메일을 남겨주시면…" + 위젯에 이메일 입력 카드 노출 |
| S2 | 카드에 이메일 저장 | 확인 메시지, 고객 생성/연결, `conversations.reply_channel='email'` |
| S3 | 상담사가 콘솔에서 답변 | **고객 이메일로 답변 수신**(테스트: dev@amoeba.group), 대화에 "이메일로 발송됨" 기록 |
| S4 | 고객이 위젯에서 다시 질문 | `reply_channel` 해제 → 이후 답변은 메일 미발송(위젯 전달) |
| S5 | 업무시간 중(뉴욕 09–12, 13–18) 에스컬레이션 | 종전대로 상담사 라우팅(메일 안내 아님) |
| S6 | 휴게시간(12–13 NY) 문의 | 오프아워와 동일 처리 |
| S7 | 콘솔 `/settings` → 상담 연결 카드 | 업무시간·요일·휴게·오프아워 메일함 저장/재조회 정상, `/ai-setting`에는 이동 안내 |
| S8 | 이메일 미보유 상태로 동의 전 저장 시도 | 403(수집 차단) |

## 3. 엣지 케이스

| ID | 케이스 | 처리 |
|---|---|---|
| E1 | SMTP 미설정 환경 | `MailerService.send`가 조용히 false — 상담·저장 흐름 영향 없음 |
| E2 | 삭제요청(Erasure) 이메일 입력 | `createFromLead`의 억제 검사로 409 — 프로필 재생성 차단(PRV-H2) |
| E3 | 콘솔 저장 시 휴게 필드 | 저장 페이로드에 항상 포함(빈 배열이라도) — 미포함 시 기존 값 소실 방지 |
| E4 | 메일 발송 실패 | 경고 로그만, 상담사 답변은 정상 저장(발송 알림 메시지는 미기록) |
| E5 | 오프아워 안내 tenant override | 이메일 보유 시 override 우선, 미보유 시 "이메일을 남겨주세요" 안내가 우선 |

## 4. 실행 기록

- 2026-08-06: U1~U8 PASS. S1~S8은 스테이징 배포(마이그레이션 선적용) 후 실측 — 결과는 RPT.
- 실메일 검증은 승인된 **dev@amoeba.group** 으로만 수행(오프아워 메일함을 임시 변경 후 원복).
