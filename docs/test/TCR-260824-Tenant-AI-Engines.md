# TCR-260824-Tenant-AI-Engines

테넌트별 AI 엔진 모델 설정 — 테스트 케이스

- 근거: `REQ-260824-Tenant-AI-Engines` / `PLN-260824-Tenant-AI-Engines`
- 대상: PR #359 (`7083b84`)

## 1. 단위 테스트 (자동, 1,543건 전체 통과 · 신규 32건)

### 1-1. 라우팅 격리 (`ai-gateway.routing.spec.ts`, 11건 중 신규 3건)
| ID | 케이스 | 기대 |
|---|---|---|
| R-1 | 다른 테넌트만 기본 엔진을 가짐 | **그 엔진을 고르지 않는다** — 플랫폼 stub 선택 |
| R-2 | 내 기본 엔진 + 플랫폼 기본 엔진 | 내 것이 이김(`TENANT_DEFAULT`) |
| R-3 | 다른 테넌트의 기본 엔진만 존재 | **`NONE`** — 남의 계정으로 답하느니 답하지 않는다 |
| R-4~11 | 기존 케이스(명시·상속·비활성 건너뛰기 등) | 무회귀 |

⚠️ 테스트 더블도 함께 고쳤습니다. 이전 더블은 "테넌트 조건이 없으면 플랫폼"으로 취급해
**결함이 있어도 통과**했습니다. 이제 `IsNull()`과 숫자를 구분합니다.

### 1-2. 테넌트 엔진 서비스 (`tenant-ai-engine.service.spec.ts`, 20건)
| ID | 케이스 | 기대 |
|---|---|---|
| E-1 | 본문에 `tenant_id`/`tenantId`를 실어 보냄 | **무시**, 호출자 테넌트로 저장 |
| E-2 | 키 없이 생성 | `status='disabled'` — 조용한 stub 폴백 방지 |
| E-3 | 어댑터 없는 프로바이더(`custom`) | 400 |
| E-4 | 키 필드 비우고 이름만 수정 | **기존 키 유지** |
| E-5 | 키를 넣어 수정 | 비활성 → 활성 승격 |
| E-6 | 남의 엔진 수정 | **404** |
| E-7 | 기본값 지정 | 같은 테넌트의 다른 기본값 해제 후 지정 |
| E-8 | 비활성 엔진을 기본값으로 | 400 |
| E-9 | 사용 중 엔진 삭제 | 거부, 삭제 미발생 |
| E-10 | `usedBy` | 보유 기능 이름 목록 |
| E-11 | 미사용 엔진 삭제 | `{id, tenantId}`로 스코프 삭제 |
| E-12 | 키 없는 엔진 테스트 | **외부 호출 없이** auth 사유 반환 |
| E-13 | 프로바이더가 404 응답 | `model` 사유 + 원문 일부 |
| E-14~20 | 실패 분류 7종(401/invalid_api_key/404/unknown model/429/overloaded/ENOTFOUND) | 각 사유로 분류 |

## 2. 배포 검증 (스테이징, 2026-08-24)

### 2-1. 배포 자체
| ID | 확인 | 결과 |
|---|---|---|
| S-1 | API 부팅 | `Nest application successfully started` |
| S-2 | 컨테이너 age | api·web 10초 — 실제 교체 |
| S-3 | 신규 라우트 | `/tenants/me/ai-engines` **401** |
| S-4 | 부팅 시 라우트 매핑 | GET·POST·PATCH·PUT(default)·POST(test)·DELETE **6개** |
| S-5 | 배포본에 D-2 수정 | `IsNull)(), isDefault: 1, status: 'enabled'` |
| S-6 | 배포본에 D-1 수정 | `listEnabledFor(tenantId)` + `IsNull)()`, 컨트롤러가 이 버전 호출 |
| S-7 | 콘솔 번들 | `billingNote` · `tenants/me/ai-engines` 포함 |

### 2-2. 결함 실증 — 임시 엔진으로 재현 후 삭제
`ai_engines`에 **tenant 2 소유·기본값·키 없음** 행을 임시로 넣고, 코드가 실행하는 쿼리를
수정 전/후 형태로 실데이터에 대조했습니다. 확인 후 삭제(잔존 0건 확인).

| ID | 쿼리 | 결과 |
|---|---|---|
| S-8 | **D-2 수정 전** `is_default=1 AND status='enabled'` | **2행** — `Built-in Stub`(NULL)과 **`TEMP`(tenant 2)**. `findOne`에 정렬이 없어 남의 엔진이 뽑힐 수 있었음 |
| S-9 | **D-2 수정 후** `+ tenant_id IS NULL` | **1행** — `Built-in Stub`만 |
| S-10 | **D-1 수정 전** `status='enabled'` (ORDER BY id DESC) | **4행** — 맨 앞이 **`TEMP`(tenant 2)**. tenant 1 선택 목록 최상단에 남의 엔진이 떴을 것 |
| S-11 | **D-1 수정 후** `+ (tenant_id IS NULL OR tenant_id=1)` | **3행** — 플랫폼 3개만 |
| S-12 | 정리 | `TEMP-%` 잔존 **0건** |

## 3. 수동 스모크 (남음 — 사람 필요)

콘솔 로그인 자격증명을 확보하지 못해 실행하지 못했습니다(§4-R1).

| ID | 시나리오 | 확인할 것 |
|---|---|---|
| M-1 | 설정 → AI 엔진 카드 | 내 엔진 없음 안내 + 플랫폼 3개가 🔒로 보이는지 |
| M-2 | 엔진 추가(키 없이) | 목록에 "API 키 없음 — 비활성"으로 표시되는지 |
| M-3 | 키를 넣어 수정 | 활성으로 바뀌는지 |
| M-4 | 이름만 수정(키 칸 비움) | **키가 유지되는지** |
| M-5 | 연결 테스트 — 잘못된 키 | "인증 실패 — API 키를 확인" 문구 |
| M-6 | 연결 테스트 — 잘못된 모델명 | "모델을 찾을 수 없음" 문구 |
| M-7 | 기본값 지정 | 배지 이동, AI 설정 화면의 `source`가 바뀌는지 |
| M-8 | 기능에 연결 후 삭제 시도 | 어떤 기능이 쓰는지 토스트에 뜨는지 |
| M-9 | 6개 언어 전환 | 신규 문구가 각 언어로(vi/ja/zh는 β) |
| M-10 | 실제 대화 | 내 엔진으로 응답이 나가는지(과금 주체 확인) |

## 4. 잔여
- **R1.** `dev@amoeba.group` 비밀번호가 세션 중 변경되어(계정은 active·`must_change_password=0`)
  인증 경로 검증을 못 했습니다. 이 스키마에는 로그인 잠금 컬럼이 없어 실패 시도로 인한
  계정 영향은 없습니다.
- **R2.** §3 전체.
