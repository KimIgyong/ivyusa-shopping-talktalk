# TCR-260904 — 사용자 매뉴얼 전면 현행화 검증

| 항목 | 내용 |
|---|---|
| 작성일 | 2026-09-04 |
| 근거 | REQ/PLN-260904-User-Manual-Refresh · PR #471 |
| 성격 | 문서+정적 자산 — 코드·스키마 무변경 (회귀 표면 = /manual 정적 서빙뿐) |

## 1. 사전 검증 (머지 전, 로컬 실측)

| # | 항목 | 방법 | 결과 |
|---|---|---|---|
| T1 | 화면 사실 정확성 | 매뉴얼 서술을 코드·i18n ko/en/vi 로케일과 대조(설정 7탭 라우트, deny-list 모드 라벨, 보드 상태 배지, 통계 섹션 라벨, 기본 시나리오 버튼 6종, 인사말 6언어 탭) | ✅ 전 항목 실제 라벨 사용 |
| T2 | 한글 잔존 (en·vi) | `[가-힣]` 전수 grep — md 6종 0건, html 6종은 언어 내비 endonym "한국어" 1건만(의도적) | ✅ |
| T3 | HTML 태그 균형 | section/div/table/ul/ol/dl/figure 스택 파서 — 9종 전부 균형 | ✅ |
| T4 | 이미지 참조 무결성 | html이 참조하는 `img/*.jpg` 전수 존재 확인(신규 figure 6개 포함) | ✅ 깨진 참조 0 |
| T5 | public 사본 링크 규약 | `GUIDE-2608*`/`docs/guide` 링크 누출 0 (신규 변경 파일 기준 — platform-integration 3종의 기존 quirk는 무변경·범위 외) | ✅ |
| T6 | 스크린샷 실화면 검증 | 로컬 dev(현행 main, 데모 데이터)에서 Playwright 캡처 — 7탭·연동 가이드·미테스트 배지·보드 문서·요금제/애드온 버튼이 프레임 안에 있는지 표본 4장 육안 확인 | ✅ |
| T7 | ko 원본 ↔ public 사본 동기 | 변환 스크립트로 재생성(링크 매핑·온라인판 주석 규약이 기존 사본과 동일 diff 형태) | ✅ |

## 2. 배포 후 확인 항목 (스테이징)

- [ ] `/manual` 인덱스 → 4개 문서 카드 정상, ko/en/vi 열람
- [ ] user-manual.ko.html §8 통계 figure·knowledge-ai.ko.html §2.0 보드 figure 렌더
- [ ] 신규 이미지 표본 `img/statistics.ko.jpg`, `img/knowledge-board.en.jpg` → 200 `image/jpeg`
- [ ] SPA 라우팅 회귀 없음(/manual은 정적, 콘솔 로그인 정상)

## 3. 알려진 편차

- 캡처는 스테이징이 아니라 **로컬 dev + 데모 데이터**(8/24 REQ-Manual-Screenshots §3의
  MFA·PII 배제 결정을 승계). 데이터 양이 적어 통계 화면은 집계 경고 배너가 보이는
  상태로 캡처됨 — 배너 자체가 문서화 대상 기능이라 수용.
- 라이브챗 캡처에는 그룹 탭·핀 아이콘이 데이터 부족으로 두드러지지 않음 — 본문
  서술로 충분하다고 판단, 재캡처 보류.
- vi html은 en 캡처를 재사용(8/24 관례 유지).
