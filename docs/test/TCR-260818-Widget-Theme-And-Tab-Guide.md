# TCR-260818-Widget-Theme-And-Tab-Guide

테넌트 테마 + 탭 설정 발견 가능성 개선의 테스트 케이스와 결과.

- 작성일: 2026-08-18
- 선행: `REQ-260818-…` → `PLN-260818-…`

## 1. 단위 테스트 (자동, 신규 18건)

| # | 케이스 | 결과 |
|---|---|---|
| U-1 | `parseHex` 3/6자리·해시 유무·공백 허용 | ✅ |
| U-2 | `parseHex` 비색상 입력 전부 거부 | ✅ |
| U-3 | 어두운 브랜드→흰 글씨, 밝은 브랜드→잉크 | ✅ |
| U-4 | **색상환 전역 스윕(24색상 × 5명도)에서 대비 3:1 이상** | ✅ |
| U-5 | **디자인 파랑에서 흰 글씨 유지** — 잉크가 대비는 높지만 뒤집지 않는다 | ✅ |
| U-6 | 램프가 위젯이 쓰는 단계를 전부 생성 | ✅ |
| U-7 | 500은 입력한 hex 그대로 | ✅ |
| U-8 | **명도 단조성**: 50이 가장 밝고 900이 가장 어둡다 | ✅ |
| U-9 | 기본 팔레트 색을 넣으면 기본 팔레트가 나온다 | ✅ |
| U-10 | 잘못된 색은 램프 대신 null | ✅ |
| U-11 | 미설정이면 변수 0개(스타일시트 기본값 유지) | ✅ |
| U-12 | 램프 + 전경색 동시 생성 | ✅ |
| U-13 | 헤더는 `brand` 선택 시에만 덮어쓴다 | ✅ |
| U-14 | 밝은 브랜드 헤더는 잉크 글자 | ✅ |
| U-15~18 | `normalizeWidgetTheme` 대문자화·확장·기본 헤더·불량 입력 null·미지 헤더 무시 | ✅ |
| U-19 | 세션이 저장 테마를 정규화해 서빙 | ✅ |
| U-20 | **불량 저장 테마는 미설정으로 강등**(위젯이 깨지지 않음) | ✅ |

전체: **1,303 + 126 통과 / 실패 0**.

## 2. 게이트

| 게이트 | 결과 |
|---|---|
| typecheck / build | ✅ 13/13 |
| `npm run i18n:check` | ✅ 5개 언어 complete |
| API 실부팅 | ✅ |
| 마이그레이션 멱등 | ✅ 재실행 exit 0 |

## 3. 통합 시나리오 (로컬 실행 검증 — 2026-08-18)

| # | 시나리오 | 기대 | 결과 |
|---|---|---|---|
| S-1 | **S1 무변화 기준선** — 변수화만 하고 테마 없음 | 빌드 CSS의 기본값이 이전 hex와 동일, 알파(`/.1`·`/.05`) 보존 | ✅ |
| S-2 | 브랜드 `#E11D6B` + 브랜드 헤더 | 런처·CTA·칩·헤더가 전부 자홍, 헤더 글자 흰색 | ✅ |
| S-3 | 브랜드 `#FFD400` + 브랜드 헤더 | 헤더 글자·아이콘이 **검정으로 자동 전환** | ✅ |
| S-4 | 상태색 고정 | success/warning/error/review가 원래 값 그대로 | ✅ DOM 실측 |
| S-5 | 테마 해제 | 인라인 오버라이드 **0개**, 팔레트 `43 127 255`로 복귀, 캐시 삭제 | ✅ |
| S-6 | 3탭 기본 | 알림·주문·채팅 노출 | ✅ |
| S-7 | 콘솔 카드 분리·테마 카드 | — | ⬜ §5 R-1 |

## 4. 엣지 케이스

| # | 케이스 | 처리 |
|---|---|---|
| E-1 | 저장된 테마가 깨진 값 | 미설정으로 강등(U-20) — 위젯은 기본 팔레트 |
| E-2 | 어두운 브랜드가 500에 오면 600이 더 밝아지는 문제 | 곡선 전체를 브랜드 명도에 맞춰 이동(단조성 U-8) |
| E-3 | localStorage 차단/사설모드 | try/catch — 기본 팔레트로 렌더 |
| E-4 | 테넌트가 테마를 껐다 | 변수 제거로 스타일시트 복귀(S-5) |
| E-5 | 첫 방문 | 캐시가 없어 기본색으로 잠깐 뜬 뒤 테마 적용(설계상 수용) |

## 5. 미검증 — 잔여

| # | 항목 | 사유 |
|---|---|---|
| R-1 | **콘솔 위젯 테마·탭 구성 카드 육안 확인** | 로그인에 비밀번호 입력이 필요해 미수행 |
| R-2 | 첫 방문 깜빡임의 체감 정도 | 실사용 관찰 필요 |
| R-3 | 6개 언어 육안 렌더 | ko만 확인 |
| R-4 | 스테이징 배포 후 회귀 | 마이그레이션 선적용 필요 |
| R-5 | 중간 명도 브랜드의 3~4.5 대비 구간 | 설계상 수용(D-10과 같은 계열), 문서화됨 |
