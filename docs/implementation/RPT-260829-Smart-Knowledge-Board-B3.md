# RPT-260829 — Smart Knowledge Board B3: 협업 (코멘트·멘션·백링크·작성 동선)

- 요구/계획/테스트: `REQ-260829-Smart-Knowledge-Board.md` → `PLN-…-B3.md`(승인) → `TCR-…-B3.md`
- **PR #448** (squash) — main `4a600c0`, 2026-08-29. 스키마 1테이블(board_comments) —
  **스테이징 SQL 선적용 → 코드 배포** 순서 준수

## 1. 무엇이 만들어졌나

1. **코멘트+@멘션**: `board_comments`(본문+정제된 테넌트 사용자 멘션 id, 상한 10 —
   타 테넌트 id는 조용히 제거). 수정 없음·삭제만(작성자/master·director), 문서 삭제 시
   동반 삭제. 작성자·멘션 이름 배치 해석(이름→이메일→`#id` 폴백).
2. **멘션함**: 보드 목록 "@나 N" 뱃지+패널(`JSON_CONTAINS` 질의, 문서 제목 동반) —
   `agent_alerts`는 대화 종속(conversation_id 필수)임을 확인해 벨/이메일 연동은 범위
   외로 확정(후속 검토 항목).
3. **백링크 그래프**(B1 links JSON의 소비 단계): 상세 패널에 Backlinks(이 문서를 링크한
   문서) + Outgoing(타깃별 작성/미작성 — 미작성은 뱃지 클릭 시 **제목·그룹 프리필 새
   문서**, 옵시디언식 생성 유도). 제목 변경은 정직 표시+안내(자동 리라이트 보류, P5-7).
4. **작성 동선(C3/D-1 이행)**: KnowledgePage [보드에 작성] primary(활성 그룹 프리셋),
   직접 추가는 ghost 격하+긴급용 안내 1줄 — 차단 없음.

## 2. 검증 결과

| 단계 | 결과 |
|---|---|
| 단위/회귀 | 코멘트 5케이스 신규 · **177 suites / 1,776 green** · typecheck · i18n · 빌드 |
| 로컬 스모크 | 멘션 정제(9999 제거)·이름 해석 → 멘션함(경계 수정 후 1건) → 링크 그래프: [[환불 예외]] 미작성 → 동명 문서 생성 → 상호 backlink/outgoing 매핑 |
| 스테이징 E2E | go2joy 문서 1: 코멘트+멘션(Notion Smoke 해석)·멘션함(문서 제목 동반)·링크 그래프([[빠른 객실 잠금 설정]] = 보드에 동명 문서 없음 → not written, 정확) |
| 콘솔 육안 | Comments 패널(멘션 칩·@ 자동완성 입력·삭제)·Links 패널(Backlinks/Outgoing·not written 뱃지·개명 안내) 스크린샷 확인 |
| 배포 | SQL 선적용(board_comments) → 머지 → 배포, `successfully started`·mentions 라우트 401 |

## 3. 실전이 잡은 결함 (당일 수정)

- **JWT userId 문자열 재발**: 멘션함이 0건 — `JSON_CONTAINS`가 `"1"`(문자열)을 찾고
  있었고, 같은 원인으로 작성자 본인 코멘트 삭제 거부 직전이었다. 보드/리뷰 컨트롤러
  `actor()`에서 `Number()` 경계 정규화로 수정. **기록된 함정(경계 id는 양변 Number)의
  재발 — 새 컨트롤러를 만들 때마다 경계 정규화를 관성으로.**
- 잔존 문구: 에디터 하단 "(backlinks coming soon)" — 본 RPT PR에서 6로케일 문구 갱신.

## 4. 잔여 (로드맵)

- **B4(마지막)**: 외부 FAQ/Q&A 게시판 임포트(일괄등록 파서 재사용, Board 타깃),
  인제스트 원본 파일(kb_files)↔보드 문서 정밀 연결.
- 후속 검토(범위 외 기록): 멘션 벨/이메일 알림, 링크 자동 리라이트, 그래프 뷰.
