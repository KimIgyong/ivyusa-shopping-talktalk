# TCR-260824 라이브챗 세션 그룹핑 (타임라인/프로젝트) — 테스트 케이스 & 결과

- 근거: `docs/plan/PLN-260824-LiveChat-Session-Grouping.md`
- 실행 환경: 로컬(dev, MySQL :3316) — 2026-08-24

## 1. 유닛 테스트 (자동, Jest)

전체 스위트: **136 suites / 1,500 tests 통과** (신규 `chat-group.service.spec.ts` 11케이스 포함, 회귀 0).

| # | 케이스 | 결과 |
|---|---|---|
| U1 | 유니크 세션 2개 미만 생성 거부(중복 id는 1개로 접힘) | ✅ |
| U2 | 타 테넌트 세션 포함 생성 거부(404) — 소유 검증 선행 | ✅ |
| U3 | 생성: 그룹 저장 + 세션당 멤버십 1행, 제목 trim | ✅ |
| U4 | 타 테넌트 그룹 id 조회 = not found (E5056) | ✅ |
| U5 | 병합 피드: 멤버 대화들의 `conversation_id IN` + 단일 id 커서(LessThan), 오름차순 반환, conv→세션·채널 매핑 | ✅ |
| U6 | 비멤버 세션으로 1:1 발신 거부(E5058), sendMessage 미호출 | ✅ |
| U7 | 발신 대상 해석: ended보다 **열린 대화방** 우선, 기존 `AgentService.sendMessage`로 위임 | ✅ |
| U8 | 수신 전용 채널(sms) 대상 발신 거부(E5058) | ✅ |
| U9 | 멤버 2명에서 제거 거부(E5057) | ✅ |
| U10 | 멤버 3명 이상이면 제거 허용 | ✅ |
| U11 | 해제: 멤버십→그룹만 삭제, 대화 테이블 미접근 | ✅ |

## 2. 빌드·부팅 검증 (자동)

| 항목 | 결과 |
|---|---|
| `npm run typecheck` (9 tasks) / `npm run build` (6 tasks) | ✅ 전체 통과 |
| `npm run i18n:check` | ✅ es/ko/vi/ja/zh complete (신규 groups.* ~44키 × 6언어) |
| 실부팅 (`node dist/main.js`, dev DB) | ✅ `successfully started` |
| 엔티티→테이블 자동 생성이 `sql/migration_chat_groups.sql` 정의와 일치 | ✅ chat_groups·chat_group_members(uq_cgm_group_session 포함) |
| 신규 라우트 무인증 401 (`/agent/groups`, `/agent/groups/:id/messages`) | ✅ 401 (404 아님) |
| `migrations:manifest` 갱신 | ✅ 64 files |

## 3. 통합/수동 시나리오 (스테이징 배포 후 실행)

| # | 시나리오 | 기대 결과 | 결과 |
|---|---|---|---|
| G1 | 위젯 세션 2개 생성(동의 후 각 1메시지) → 목록 [선택] → 2개 체크 → 타임라인 생성 | 그룹 탭에 [타임라인] 제목·멤버 2 표시 | ⬜ |
| G2 | 그룹 룸 진입 | 두 세션 메시지가 시간순 단일 스트림, 메시지별 세션 표기+채널 배지 | ⬜ |
| G3 | 수신 대상 미선택 상태 | 입력창 비활성·발신 불가 | ⬜ |
| G4 | 세션 A 선택 → 발신 | A 위젯에만 수신(B 위젯 0건), 그룹 룸 피드에 표시 | ⬜ |
| G5 | 멤버 세션에서 대화 종료 후 고객이 새 메시지 → 새 대화방 생성 | 새 대화도 그룹 룸에 자동 유입(D1) | ⬜ |
| G6 | 멤버 2명 그룹에서 제거 시도 | 거부(E5057) + 해제 안내 문구 | ⬜ |
| G7 | 그룹 해제 | 그룹만 사라지고 세션·대화·메시지는 목록에 그대로 | ⬜ |
| G8 | 타 테넌트 세션 id로 `POST /agent/groups` 직접 호출 | 404 + 서버 warn | ⬜ |
| G9 | 타 테넌트 토큰으로 그룹 조회/발신 | 404 (E5056) | ⬜ |
| G10 | 기존 그룹에 세션 추가(선택 모드 → 기존 그룹에 추가) | 멤버 수 증가, 병합 피드에 해당 세션 메시지 유입 | ⬜ |

## 4. 엣지 케이스 (설계로 처리)

- 같은 세션의 행을 여러 개 체크: sessionId 기준 Map이라 멤버 1명으로 접힘.
- 같은 세션을 같은 그룹에 재추가: DB 유니크 + 서비스 중복 스킵(에러 아님).
- 멤버 세션에 대화가 0건: 발신 시 대상 대화방 없음 → E5058(이론상 목록에 노출되지 않는 세션이라 실경로 없음).
- 그룹 제목 100자 초과: DTO MaxLength + 서버 slice 이중 방어.
- 유형 변경(타임라인↔프로젝트): 라벨만 바뀌고 어떤 동작도 변하지 않음(구분자 원칙).
