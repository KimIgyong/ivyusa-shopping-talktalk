# TCR-260809-Issue-Workflow-P3

PLN-260809-Issue-Workflow-P3 — PR #201(백엔드)/#202(위젯) 테스트 케이스·결과.

## 1. 단위 (jest — 805/805 PASS, P3 신규 5케이스)
| # | 케이스 | 결과 |
|---|---|---|
| U1 | 이슈 생성·전이마다 세션 언어 로컬라이즈 알림 발행(category issue, push, #번호 포함) | ✅ |
| U2 | 반려 알림은 사유별 문구(정책상 불가 등) | ✅ |
| U3 | 웹훅: 미지 토큰 → false(컨트롤러 401) | ✅ |
| U4 | 웹훅: closed 미러링 + 고객 알림 1회 발행 | ✅ |
| U5 | closed 티켓 재-에스컬레이션 → 동일 ref로 **신규 티켓** 생성(결정 12 완성) | ✅ |

## 2. 로컬 통합
| # | 케이스 | 결과 |
|---|---|---|
| I1 | `sql/260809-issue-p3.sql` dev 적용 + 실부트(신규 컨트롤러 2종 매핑 확인) | ✅ |

## 3. 스테이징 (2026-08-09 배포)
| # | 케이스 | 결과 |
|---|---|---|
| S1 | SQL 선적용(external_tickets.status) → 배포, 부트 정상·스키마 에러 0 | ✅ |
| S2 | `GET /issues` live(가짜 토큰 → E3001) / 웹훅 bad token → **401** | ✅ |

## 4. 수동 E2E (사용자 스모크 — 잔여)
| # | 시나리오 | 기대 |
|---|---|---|
| E1 | amoebaorder 위젯 에스컬레이션 → 알림 탭 | "문의가 접수되었습니다 (#N)" 인앱+웹푸시 |
| E2 | 상담원 수락→해결→종료 | 각 전이마다 알림 수신 |
| E3 | 주문 탭 '문의' 서브탭 | 이슈 피드(#N·유형·상태 뱃지·안내문) 표시, 15초 내 상태 갱신 |
| E4 | 이슈 없는 세션/테넌트의 '문의' 서브탭 | 기존 주문 필터 그대로(P3 이전과 동일) |
| E5 | 반려(정책상 불가) | 사유별 안내 알림 |
| E6 | (Gorgias 실 계정) webhook_secret 저장 + HTTP Integration 설정 → 티켓 close | 위젯 "처리 완료" 알림; 이후 재문의 → 새 티켓 |

## 5. 메모
- 문의 피드는 주문 탭 내라 로그인 후 노출 — 게스트 세션 이슈는 알림으로만 회신(피드 게스트 노출은 후속 검토).
- email 회신은 replyChannel=email 스레드의 해결/반려에만(결정 6), SMS 없음.
