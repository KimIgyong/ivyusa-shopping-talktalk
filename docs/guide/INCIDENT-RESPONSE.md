# 개인정보 침해 사고 대응 런북 — Incident Response Runbook (Doc-C)

| 항목 | 내용 |
|---|---|
| 문서 ID | GUIDE-Incident-Response-20260731 |
| 작성일 | 2026-07-31 |
| 범위 | ShopTalk 전 환경(스테이징 `shoptalk.amoeba.site`, 향후 프로덕션)의 개인정보 침해·유출 의심 사고 |
| 기준 | `reference/amoeba_privacy_compliance_v2.md` §8 (PRV-041 사고대응 문서화, PRV-032 GDPR 72시간 통지) · REQ-Privacy-Control-Gap-20260731 #7 |
| 소유자 | 사고 지휘(Incident Commander, TBD/지정 필요) |

---

## 1. 심각도 분류

| 등급 | 정의 | 예시 | 초동 대응 시한 |
|---|---|---|---|
| **SEV1** | 개인정보 **유출 확인**(외부 반출 증거) 또는 대규모(다수 정보주체/테넌트 전체) 노출, 암호화 키(`CRED_ENC_KEY`, `JWT_SECRET`) 탈취 | DB 덤프 유출, 크로스-테넌트 데이터 노출, 키 노출 커밋 | 즉시(1시간 내 지휘체계 소집) |
| **SEV2** | 유출 **가능성 높음** — 취약점 악용 흔적, 자격증명 도용 의심, 소수 정보주체 노출 | 관리자 계정 탈취 의심, 인증 우회 발견, 로그에 비정상 DSAR/export 대량 호출 | 4시간 내 |
| **SEV3** | 유출 정황 없는 보안 이벤트 — 정책 위반, 스캔 시도, 내부 오조작(즉시 회수) | 잘못된 수신자에게 내부 자료 발송 후 회수, 반복 로그인 실패 | 1영업일 내 |

판정 기준 2축: **① 개인정보가 실제로 조직 밖으로 나갔는가 ② 영향 정보주체 규모**(1명 / 소수 / 대량·테넌트 전체). 판단 불가 시 상위 등급으로 취급한다.

## 2. RACI

| 활동 | 사고 지휘(IC) | 기술 조사 | 법무·통지 | 커뮤니케이션 |
|---|---|---|---|---|
| 등급 판정·전파 | **R/A** | C | C | I |
| 격리·복구 | A | **R** | I | I |
| 증거 보존 | A | **R** | C | — |
| 감독기관·정보주체 통지 | A | C | **R** | C |
| 고객(테넌트)·대외 공지 | A | C | C | **R** |
| 사후 검토(포스트모템) | **R/A** | R | C | C |

담당자(placeholder — 지정 후 갱신):

| 역할 | 성명 | 연락처 |
|---|---|---|
| 사고 지휘(IC) | 〔지정 필요〕 | |
| 기술 조사 | 〔지정 필요〕 | |
| 법무·통지 | 〔지정 필요〕 | |
| 커뮤니케이션 | 〔지정 필요〕 | |

## 3. 단계별 절차

### 3.1 탐지

- 모니터링: API 부트/에러 로그(`docker logs`), `audit_logs` 이상 패턴(대량 `dsar.export`, `gdpr.*`, 비정상 시간대 privileged action), `moderation_logs` 급증.
- 주의: **4xx는 서버 로그에 기본 기록되지 않는다**(CLAUDE.md §2) — "로그에 없음 ≠ 시도 없음".
- 제보 경로: 내부(팀 채널/이메일 — TBD), 외부 제보 창구(보안 이메일 — TBD/개설 필요), 테넌트 신고.
- 접수 즉시 사고 티켓 생성(시각·제보자·최초 관측 내용) → IC가 §1 기준으로 등급 판정.

### 3.2 격리 — 이 저장소의 실제 수단(코드 근거)

| 수단 | 방법(코드 근거) |
|---|---|
| **JWT 전면 무효화** | 해당 계정 비밀번호 변경 → `auth.service.ts`가 `password_changed_at`을 스탬프하고, 토큰 검증 시 `assertIssuedAfterPasswordChange`가 그 이전 발급(iat) 토큰을 전부 거부. 전 계정 무효화가 필요하면 `JWT_SECRET` 교체 후 API 재기동 |
| **위젯 세션 무효화** | `sessions` 행 삭제(또는 `session_token` 회전) + Redis 캐시 `sess:tok:{token}` 삭제(`sessionCacheKey`). 캐시 TTL 30초이므로 지연은 최대 30초 |
| **연동 자격증명 회전** | `integration_credentials.secret_enc` 재발급(콘솔/API 경유, AES-256-GCM 재암호화), `ai_engines.api_key_encrypted` 갱신, env 기반 키(`ANTHROPIC_API_KEY`, `VOYAGE_API_KEY`, `SLACK_WEBHOOK_URL`, `SMTP_*`) 회전 후 재배포 |
| **암호화 키 유출 시** | `CRED_ENC_KEY`(PII·자격증명 암호화 + `email_hash` HMAC 키) 회전 — 단순 env 교체만으로는 기존 암호문 복호화 불가이므로 **재암호화 마이그레이션 필요**(사전 리허설 없이는 SEV1 상황에서 수행 금지, 별도 런북 필요 — 미결) |
| **네트워크 격리** | 스테이징 DB는 이미 루프백 바인딩(`127.0.0.1:3317`) — 유출 경로가 호스트 계정이면 SSH 키 회수(`secrets/staging-server.md` 참조), 필요 시 컨테이너 중지 |
| **외부 전송 차단** | AI 경유 유출 의심 시 `ANTHROPIC_API_KEY`/`VOYAGE_API_KEY` 제거(스텁 어댑터로 degrade, 서비스는 유지됨), Slack/SMTP env 제거 |

### 3.3 조사

- 1차 소스: `audit_logs`(actor_type/actor_id/action/target — DSAR·GDPR·retention·privileged action이 모두 기록됨), API 컨테이너 로그, nginx 접근 로그, `moderation_logs`, MySQL 계정/접속 이력.
- 확인 사항: 침입 시각·경로, 접근된 테이블/행 범위, **영향 정보주체 수 및 항목**(통지 판단의 핵심), 크로스-테넌트 여부, 유출 데이터의 암호화 여부(customers PII는 AES-256-GCM 암호문 — 키 미탈취 시 통지 의무 경감 가능성, 법무 판단).

### 3.4 복구

- 침해 경로 패치 → 스테이징 검증 → 배포(배포 검증 규칙: boot 로그 `successfully started`, 컨테이너 age, 라우트 401/404/502 판별 — CLAUDE.md §7).
- 회전한 자격증명으로 연동 재개, 세션/토큰 정상 발급 확인, 백업 부재 상태이므로 데이터 복구는 현행 볼륨 기준(Doc-A G-8 — 백업 도입 전까지 복구 수단 제한적임을 인지).

### 3.5 통지

| 대상 | 기한/기준 | 비고 |
|---|---|---|
| GDPR 감독기관 | 인지 후 **72시간 이내**(PRV-032) — 유출이 정보주체 권리·자유에 위험을 초래할 개연성이 있는 경우 | EU 정보주체 데이터 포함 시. 리드 감독기관 특정 TBD |
| GDPR 정보주체 | 고위험 시 지체 없이 | |
| CCPA/캘리포니아 | Cal. Civ. Code §1798.82 — 주민 대상 지체 없는 통지, **500명 초과 시 캘리포니아 법무장관 제출**. 암호화 데이터+키 미탈취는 통지 예외 가능(법무 판단) | 미국 타 주법 병행 검토(주별 상이) |
| PIPA(한국 정보주체 포함 시) | 유출 인지 후 72시간 내 통지·신고(개인정보보호위원회) | |
| 테넌트(고객사) | 계약 SLA 또는 지체 없이 — 테넌트 데이터 영향 시 필수 | |
| 외부 처리자 경유 유출 | Shopify: 파트너 지원 채널(TBD/확인 필요) · Anthropic: 보안 연락처(TBD/확인 필요) · Voyage: (TBD/확인 필요) — Doc-B 대장의 사고 통지 조항과 연동 | |

모든 통지는 법무·통지 담당(R)이 작성하고 IC가 승인한다. 통지 여부와 무관하게 **판단 근거를 기록**한다(PRV-032 "및 기록").

### 3.6 사후 검토

- 사고 종결 후 5영업일 내 포스트모템: 타임라인, 근본 원인, 영향 범위, 잘된 점/개선점, 액션 아이템(담당·기한).
- 산출물은 버그픽스 워크플로우에 따라 `docs/bug-fix/FIX-{Topic}-{YYYYMMDD}.md`로 남기고, 일반화 가능한 예방 패턴은 dev-kit/메모리로 승격.

## 4. 증거 보존 규칙

- 격리 조치 **전에** 가능하면 상태 스냅샷 확보: 관련 `audit_logs`/서버 로그 export, 컨테이너 로그 덤프, 의심 세션/계정 행의 사본. 원본 테이블은 변조하지 않는다(조사는 사본으로).
- 증거 파일은 접근 통제된 별도 위치에 사고 티켓 ID로 보관, 해시(sha256) 기록.
- 로그 로테이션으로 인한 소실 방지: 사고 인지 즉시 해당 기간 로그를 먼저 복사(현재 로테이션 정책 미정 — Doc-A G-7).
- 증거 내 PII는 조사 목적 외 열람 금지, 종결 후 보존기간(법무 지정, TBD) 경과 시 파기.

## 5. 훈련 일정 템플릿

| 구분 | 주기 | 내용 |
|---|---|---|
| Tabletop(도상훈련) | **반기 1회** | 시나리오 기반 역할 점검(예: ① CRED_ENC_KEY 커밋 유출 ② 상담원 계정 탈취로 타 테넌트 열람 ③ Anthropic 측 사고 통지 수신) — Doc-B 반기 재검증과 동일 주기로 병행 |
| 모의훈련(기술 실행) | **연 1회** | 실제 격리 절차 리허설: 테스트 계정 비밀번호 회전→JWT 무효화 확인, 세션 캐시 무효화, 스테이징 자격증명 회전, 72h 통지 문안 작성까지 |

### 훈련 기록 표

| 일자 | 구분 | 시나리오 | 참여자 | 발견된 미비점 | 후속 조치(담당/기한) |
|---|---|---|---|---|---|
| | | | | | |
| | | | | | |
