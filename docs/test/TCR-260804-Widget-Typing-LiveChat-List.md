# TCR — 위젯 타이핑 인디케이터 + /live-chat 목록 개선 (2026-08-04)

> 근거: `docs/plan/PLN-260804-Widget-Typing-LiveChat-List.md` (승인: 전체 + 인덱스 + 대화방 5s 갱신)

## 1. 단위 테스트 (jest, apps/api — 신규 `agent.service.listsessions.spec.ts`)

| ID | 케이스 | 결과 |
|---|---|---|
| U1 | `listSessions` q 없음 → 무필터 큐 목록, 고객검색 미호출 | PASS |
| U2 | `listSessions` q 매칭 → `searchByEmailOrName(≤20)` → 세션 필터로 대화 조회 | PASS |
| U3 | q가 아무 고객도 못 찾으면 `{items:[], total:0}` + conversations 쿼리 생략 | PASS |
| U4 | `toSessionResponse`가 `lastMessageAt`/`lastMessagePreview` 전달 | PASS |
| U5 | 메시지 없는 대화 → 둘 다 null | PASS |

전체 스위트 **47 suites / 490 tests PASS**, `npm run typecheck`/`build` 통과,
API 실부팅 확인(엔티티 인덱스 변경 A-1 체크).

## 2. 통합 시나리오 (스테이징 실측)

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| S1 | 위젯에서 메시지 전송(AI 모드) | 전송 즉시 ●●● "답변을 작성 중입니다…" 표시 → AI 답변 도착 시 교체 |
| S2 | 상담사 연결(waiting/agent) 상태에서 고객 메시지 전송 | ●●● "담당자가 답변을 작성 중입니다…"가 상담사 답변 폴링 수신까지 유지 |
| S3 | /live-chat 목록 | 각 행에 미리보기(더 이상 '—' 아님) + "개설 Nm ago · 마지막답변 Nm ago" (툴팁 절대시각) |
| S4 | 고객명 검색 박스에 이름 입력 | 해당 고객 대화만 필터, 미검색 시 전체 |
| S5 | 목록 갱신 | 새 에스컬레이션이 5초 내 목록 반영(종전 15초) |
| S6 | 열린 대화방 | 고객 새 메시지가 상담사 조작 없이 5초 내 자동 표시 |
| S7 | `?q=` 무매칭 | 빈 목록 |
| S8 | 언어 en/es/ko | 인디케이터·목록 라벨 3개 언어 정상 |

## 3. 엣지 케이스

| ID | 케이스 | 처리 |
|---|---|---|
| E1 | 전송 실패 | 인디케이터 소멸 + 기존 실패 버블(현행) |
| E2 | 검색어 있는 채로 5s 폴링 | queryKey에 q 포함 — 검색 결과가 주기 갱신됨 |
| E3 | 암호화 제약 | 이름 검색은 최근 고객 500명 창(기존 고객검색과 동일) — 검색박스 툴팁에 명시 |
| E4 | 인덱스 미적용 상태로 코드 선배포 | 동작 동일(성능만 저하) — 단 규정상 SQL 선적용 |
| E5 | agent 모드에서 마지막 메시지가 상담사/시스템 | 인디케이터 미표시(고객 메시지가 마지막일 때만) |

## 4. 실행 기록

- 2026-08-04: U1~U5 PASS. S1~S8은 스테이징 배포(인덱스 선적용) 후 실측 — 결과 RPT에 기록.
