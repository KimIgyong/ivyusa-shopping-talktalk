# TCR-260809-Issue-Workflow-P5

PLN-260809-Issue-Workflow-P5 — PR #208 테스트 케이스·결과.

## 1. 단위 (jest — 812/812 PASS, P5 신규 7케이스)
| # | 케이스 | 결과 |
|---|---|---|
| U1 | 배치: 에스컬레이션 다발 클러스터(asked≥3∧rate≥0.5) 제안 | ✅ |
| U2 | 배치: 임계 미달 스킵 + no-source(≥3) 인텐트 제안 | ✅ |
| U3 | 멱등: 기각/승인된 태스크는 재제안 안 함(결정 9) | ✅ |
| U4 | 상담원 해결 → 질문(PII 스크럽 제목)+답변 후보 제안 | ✅ |
| U5 | 상담원 답변 없으면 제안 생략 | ✅ |
| U6 | 승인: (편집된) 제목/본문으로 기존 createDocument+임베딩 호출, accepted 마킹 | ✅ |
| U7 | 본문 없는 승인 400 / 이미 결정된 태스크 재결정 404 | ✅ |

## 2. 로컬 통합 / 스테이징 (2026-08-09 배포)
| # | 케이스 | 결과 |
|---|---|---|
| I1 | SQL dev·staging 적용(knowledge_gap_tasks) + 실부트 정상 | ✅ |
| S1 | `GET /knowledge/gap-tasks` → 401(배포·인증 요구), 스키마 에러 0 | ✅ |

## 3. 수동 E2E (사용자 스모크 — 잔여)
| # | 시나리오 | 기대 |
|---|---|---|
| E1 | amoebaorder 이슈를 상담원이 답변 후 해결 | /knowledge 상단 "지식 갭 제안"에 상담원 해결 카드(질문 제목+답변 미리보기) |
| E2 | 제목/본문 편집 → [문서 생성] | KB 문서 생성·임베딩, 문서 목록 반영, 카드 소멸 |
| E3 | 동일 질문을 위젯에서 재질문 | 2차 AI가 신규 문서 인용(또는 답변재사용 히트) — 폐루프 확인 |
| E4 | [기각] 후 재해결 반복 | 동일 이슈 재제안 없음 |
| E5 | (데이터 축적 후) 일배치 | 이관 다발/no-source 제안 생성 |

## 4. 메모
- 배치는 기본 24h(env로 조정/비활성) — 스테이징 즉시 확인은 E1(이슈 해결 경로)이 빠름.
- 승인 문서 category='faq', source='knowledge_gap' — /knowledge 필터로 추적 가능.
