# TCR-260825-Customer-Journey-Report

고객여정분석 리포트 — 테스트 케이스

- 근거: `REQ/PLN/GUIDE-260825-Customer-Journey-Report`
- 대상: PR #377(W1 집계) · #378(W2 서술) · #381(W3 콘솔) · #383(기준 편집)

## 1. 단위 테스트 (자동, 1,599건 전체 통과 · 신규 20건)

### 1-1. 해결 판정 (`journey-metrics.service.spec.ts`, 7건)
이 기능에서 가장 틀리기 쉬운 곳이라 케이스를 촘촘히 잡았습니다.

| ID | 케이스 | 기대 |
|---|---|---|
| R-1 | CSAT 응답 있음 | **해결** — 고객이 끝났다고 말했으므로 다른 추론이 필요 없음 |
| R-2 | 상담원이 종료 | 해결 |
| R-3 | 묻고-닫음 + **우리가 마지막 발화** | 해결 |
| R-4 | 묻고-닫음 + **고객이 마지막 발화** | **미해결** — 같은 문구, 같은 상태, 정반대 의미 |
| R-5 | 시스템 안내를 "우리 발화"로 세지 않음 | 미해결 유지 — 세면 묻고-닫음 **전부가 해결로 뒤집힘** |
| R-6 | 진행 중(ended 아님) | 미해결(open) — 빠른 해결로 세지 않음 |
| R-7 | 종료됐으나 `ended_at` 없음 | 미해결(abandoned) |

### 1-2. 파생 지표 (5건)
| ID | 케이스 | 기대 |
|---|---|---|
| M-1 | 왕복 = 발화자 전환 수 | 메시지 수가 아님 |
| M-2 | 시스템 메시지가 왕복을 만들지 않음 | 0 |
| M-3 | 중앙값이 긴 꼬리에 안 끌림 | `[5,6,7,8,40000]` → 7 |
| M-4 | 짝수 개 중앙값 | 가운데 두 값 평균 |
| M-5 | 빈 입력 | **null** — 0분은 즉시 해결로 읽힘 |

### 1-3. 프롬프트 규칙 (`journey-prompt.spec.ts`, 8건)
문구가 조용히 빠지면 리포트는 여전히 그럴듯하게 나오고 틀렸다는 걸 알 방법이 없어, 규칙 자체를 테스트합니다.

| ID | 케이스 | 기대 |
|---|---|---|
| P-1 | 숫자 생성 금지 | `copied from the METRICS block` · `Do not compute` |
| P-2 | Aware/Appeal 관측 불가 명시 | 프롬프트에 포함 |
| P-3 | 매슬로우 단정 금지 + 반증 조건 | 포함 |
| P-4 | 금지 표현 전달 | 기준의 `banned` 반영 |
| P-5 | 집계값이 유일한 숫자 원천 | METRICS 블록에 실림 |
| P-6 | 기준에서 뺀 절은 리포트에서도 빠짐 | 모델이 채우지 않음 |
| P-7 | 비교 시 기준 버전 상이 → 그 사실로 시작 | 포함 |
| P-8 | 버전 같으면 언급 없음 | 미포함 |

## 2. 배포 검증 (스테이징, 2026-08-25)

| ID | 확인 | 결과 |
|---|---|---|
| S-1 | **SQL 선적용** | 두 테이블 생성 후 코드 배포 |
| S-2 | 부팅·컨테이너 | `successfully started`, 신규 교체 |
| S-3 | 신규 라우트 | `/journey/criteria` **401** |
| S-4 | **집계 정확성(실데이터)** | §2-1 |

### 2-1. 실데이터 대조 — 그룹 2(annhearts, tenant 3)
컴파일된 집계 서비스를 스테이징 DB에 직접 붙여 돌리고 SQL과 맞췄습니다.

| 항목 | 서비스 | SQL | |
|---|---:|---:|---|
| 세션 | 4 | 4 | ✓ |
| 대화 | 17 | 17 | ✓ |
| 메시지 | 3,223 | 3,223 | ✓ |
| 고객 발화 | 3,156 | 3,156 | ✓ |
| 상담원+AI | 17 | 15+2 | ✓ |
| 채널 | sms 2 · kakao 2 | 동일 | ✓ |
| 미해결(open) | 4 | waiting 4 | ✓ |
| 종료 13 분해 | 해결 2 + customer_last 11 | ended 13 | ✓ |

### 2-2. ⚠️ 정의가 만든 차이 — 실측
```
"종료 = 해결"로 셌다면   13건 (해결률 100%)
실제 규칙으로 세면        2건 (해결률 15%)
```
**6.5배.** REQ에서 경고한 "방치된 대화가 가장 빠른 해결로 집계된다"가 실데이터에서 그대로
확인됐습니다. 이 그룹은 고객이 마지막으로 말하고 답을 못 받은 채 닫힌 대화가 대다수인데,
순진한 정의로는 **우수 사례로 보였을 것**입니다.

## 3. 수동 스모크 (남음 — 사람 필요)

| ID | 시나리오 | 확인할 것 |
|---|---|---|
| H-1 | 그룹 열기 | 우측이 **AI 브리핑 → 고객여정분석**으로 바뀌는지 |
| H-2 | 대화 1건 열기 | 브리핑이 **그대로** 있는지(그룹에서만 교체) |
| H-3 | 리포트 생성 | 접수 토스트 → 목록에 `작성 중` → 폴링으로 `완료` 전환 |
| H-4 | **본문 숫자 대조** | 리포트 본문의 수치가 `metrics_json`과 일치하는지 — **모델이 산수에 손대지 않았는지 확인하는 유일한 방법** |
| H-5 | 5A 절 | Aware·Appeal이 **"관측 범위 밖"**으로 적혔는지 |
| H-6 | 욕구 단계 절 | 인용·가설·**반증 조건** 3종이 있는지 |
| H-7 | 해결 절 | **집계 정의가 본문에 인쇄**됐는지 |
| H-8 | 심화분석 | 2건 선택 → 버전 다르면 경고 → 생성 |
| H-9 | 기준 편집 | 저장 시 버전 증가, **기존 리포트 불변** |
| H-10 | 기간 지정 | 범위 밖 세션이 빠지는지 |
| H-11 | 모더레이션 차단 | 실패 사유가 목록에 남고 **본문이 저장되지 않는지** |
| H-12 | 6개 언어 | 신규 `journey` 문구(vi/ja/zh는 β) |

## 4. 잔여
- **R1.** **end-to-end 리포트 생성 미검증.** 콘솔 로그인 자격증명을 확보하지 못했습니다
  (`gray.kim@`·`dev@`에 알려진 비밀번호 3종 모두 실패). **H-4가 가장 중요한 미검증 항목**입니다.
- **R2.** §3 전체.
- **R3.** `primaryChannel`은 동수일 때 임의로 갈립니다(실측에서 sms 2 / kakao 2). 리포트가
  "주 채널"을 단정하지 않도록 문구 확인 필요.
