# TCR-260818-Widget-Orders-Tab

주문탭 주문 목록 노출 테스트 케이스.

- 작성일: 2026-08-18
- 선행: `REQ-260818-Widget-Orders-Tab.md` → `PLN-260818-Widget-Orders-Tab.md`

## 1. 단위 테스트 (자동, 신규 15건)

### 1-1. `packages/types/src/common/order-status.spec.ts` (10건)

위젯에는 테스트 러너가 없어, 핀으로 박아둘 가치가 있는 순수 규칙만 공유 패키지로 옮겼다.

| ID | 케이스 | 결과 |
|---|---|---|
| U-1 | 우리가 쓰는 상태(`paid`/`shipping`/`delivered`/`refunded`) 매핑 | ✅ |
| U-2 | 플랫폼 동의어 흡수 — `Confirmed`→paid, `CANCELED`(L 하나)→cancelled, 공백 포함 `  processing  `→preparing | ✅ |
| U-3 | 모르는 상태는 **추측하지 않고 null** | ✅ |
| U-4 | 아는 상태는 번역 키로 | ✅ |
| U-5 | **모르는 상태는 플랫폼 원문으로 폴백** — 빈 뱃지 금지 | ✅ |
| U-6 | 원문이 공백이면 내부 코드로 폴백 | ✅ |
| U-7 | 상태가 아예 없을 때만 null | ✅ |
| U-8 | 배송중 판정은 두 필드 중 하나만 맞아도 true | ✅ |
| U-9 | **결제완료는 배송중이 아니지만 목록에는 남는다** (이번 결함의 핵심) | ✅ |
| U-10 | 취소 주문은 배송중 아님 | ✅ |

### 1-2. `apps/api/.../order.service.listforsession.spec.ts` (5건)

| ID | 케이스 | 결과 |
|---|---|---|
| U-11 | `customer_id` + **`tenant_id`** 두 조건이 모두 걸린다 | ✅ |
| U-12 | 세션에 tenant가 없으면(레거시) 조건을 빼고 **목록은 그대로 반환** — `IS NULL`을 걸면 전부 사라진다 | ✅ |
| U-13 | **서버는 상태로 거르지 않는다** — paid·shipping 모두 반환 | ✅ |
| U-14 | 기간 조건은 요청했을 때만 붙는다 | ✅ |
| U-15 | 상한(90) 초과 요청은 **조용히 깎지 않고 예외** | ✅ |

⚠️ 픽스처의 `id`는 **문자열**이다. TypeORM이 bigint PK를 문자열로 돌려주고, 숫자 픽스처가
과거에 프로덕션 불일치를 가린 적이 있다.

## 2. 게이트

| 항목 | 결과 |
|---|---|
| `npm run typecheck` | ✅ 9/9 |
| `npm run build` | ✅ 6/6 |
| `npm test` | ✅ **1,445건** — api 1,308 + common 60 + types 77 |
| `npm run i18n:check` | ✅ en 기준, es/ko/vi/ja/zh 5개 complete |

## 3. 통합 시나리오 (스테이징 수동)

전제: 위젯 `ambshop-dev.myshopify.com`, 로그인 상태.

| ID | 시나리오 | 기대 |
|---|---|---|
| S-1 | **고객 8로 로그인 → 주문탭** | `#1004 / 결제완료 / $749.95 / 2026-08-10` 노출. **이번 제보의 재현 계정이라 이게 통과해야 해결이다** |
| S-2 | 주문탭 진입 시 첫 칩 | `주문내역`이 선택돼 있고 목록이 바로 보임 |
| S-3 | 배송중 주문(고객 4·5·6) | 행 안에 배송 단계 바 표시 |
| S-4 | 결제완료 주문 | 상태 뱃지만, 단계 바 없음 |
| S-5 | 행 클릭 | 주문 상세 진입(품목·금액·배송조회·리뷰·문의) |
| S-6 | 상세 → 배송 조회 | 단계 + 택배사/송장 |
| S-7 | 상세 → 뒤로 | 주문 목록으로 복귀 |
| S-8 | `배송` 칩 | 기존 라이브 추적 뷰 그대로(회귀 없음) |
| S-9 | `리뷰`·`문의` 칩 | 기존 동작 그대로 |
| S-10 | 주문 0건 고객(7·9·18·51) | "최근 90일 주문이 없습니다" + 마이페이지 링크 |
| S-11 | 비로그인 | 기존 로그인 유도 화면 |
| S-12 | 언어 전환 6종 | 상태 뱃지·빈 문구가 해당 언어로 |
| S-13 | 주문탭 OFF 테넌트 | 알림탭이 `주문내역` 칩까지 흡수 |
| S-14 | 채팅 인라인 주문 카드 | 30일·10건 그대로(무변경) |

## 4. 엣지 케이스

| ID | 케이스 | 처리 |
|---|---|---|
| E-1 | 몰 고유 상태(`At customs` 등) | 원문 그대로 표시, 뱃지 색은 `default` |
| E-2 | `orderedAt` NULL(시드/구 데이터) | `createdAt`으로 대체 표시 |
| E-3 | `firstItemTitle` NULL | "N개 상품"으로 대체 |
| E-4 | 20건 꽉 참 | 하단에 마이페이지 링크 노출. 20건 미만이면 **숨김** — 없는 주문이 더 있다고 읽히면 안 됨 |
| E-5 | 추적 요청 실패 | 행은 그대로, 단계 바만 미표시 |
| E-6 | `trackingSteps` 키 손상 | 배열 검증 후 빈 배열(기존 방어 재사용) |
| E-7 | 저장된 칩이 `payment` | 스토어가 localStorage 영속이 아니라 **이슈 없음**(매 로드 `all`에서 시작 → `chipBelongsTo` false → `주문내역`으로 폴백). PLN S2의 우려 해소 |

## 5. 미검증

- S-1~S-14 전부 (스테이징 배포 후 수행)
- 6개 언어 육안은 ko/en만 예정
