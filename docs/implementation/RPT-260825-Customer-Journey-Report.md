# RPT-260825-Customer-Journey-Report

고객여정분석 리포트 — 구현 결과

- 근거: `REQ/PLN/GUIDE/TCR-260825`
- 상태: **스테이징 배포 + 실제 생성 검증 완료 2026-08-25**

## 1. 배포 상태

| PR | 내용 | 커밋 |
|---|---|---|
| #375 | REQ + 기능정의서 | `5a0507f` |
| #376 | PLN | `54fc65b` |
| #377 | W1 — 집계·기준 | `cfade6c` |
| #378 | W2 — 서술·비동기·모더레이션 | `fe87ab0` |
| #381 | W3 — 그룹 패널·리포트 뷰어 | `1211125` |
| #383 | W3b — 기준 편집 화면 | `6526d01` |

- 스테이징 배포 완료. **SQL 선적용 → 코드 배포** 순서 준수
- 프로덕션: 미배포 환경. 배포 시 동일 순서 필요
- 롤백: `DROP TABLE journey_reports, journey_report_criteria;`

## 2. 요청과 실제 작업의 차이

데이터는 대부분 이미 있었습니다 — `chat_groups`가 범위를, `cjm_events.stage`가 여정 단계를,
`conversations`가 CSAT와 종료 시각을 들고 있었습니다. 새로 만든 것은 **저장소·기준 관리·서술**,
그리고 **"해결"의 정의**입니다.

## 3. 설계 판단 기록

- **숫자는 코드가, 서술은 모델이.** 프롬프트가 "모든 숫자는 METRICS에서 복사하라, 없으면
  측정되지 않았다고 말하라"를 명시합니다. 모델에게 세게 하면 그럴듯하고 틀린 숫자가 나오고,
  **리포트라는 형식이 그것을 근거처럼 보이게** 만듭니다.
- **비동기.** 큰 그룹은 수십 초, nginx는 60초에 끊습니다 — 카탈로그 변환이 이미 겪은 함정.
- **행이 곧 작업.** 리포트가 결과물이라 메모리에 둘 수 없습니다. 재시작 유실을 막기 위해
  부팅 시 30분 넘은 `pending`을 사유와 함께 `failed`로 정리합니다 — *"아직 생성 중"*과
  *"한 시간 전에 죽음"*은 운영자가 할 일이 완전히 다릅니다.
- **모더레이션 차단 시 저장 안 함.** 구멍 난 리포트보다 사유가 적힌 실패가 낫습니다.
- **샘플은 기간 전체에 고르게.** 최신 N개는 운영자가 방금 읽은 것입니다.
- **세션 스냅샷.** 그룹은 뷰라 구성원이 바뀝니다. 없으면 같은 리포트를 다시 못 만듭니다.
- **기준은 최대 버전 = 현재.** 활성 플래그를 따로 두면 두 번째 진실 원천이 되고, 과거 리포트가
  자기 버전을 고정하므로 둘이 어긋나면 무엇이 실제로 썼는지 알 수 없습니다.
- **비교 시 버전 상이를 먼저 말합니다.** 없으면 **우리 규칙이 바뀐 것이 고객이 바뀐 것으로**
  읽힙니다.
- **하지 않은 것**: 점수화된 관계 등급, 개인 심리 프로파일링, Aware/Appeal 추정, 매슬로우 단정.

## 4. 실데이터가 설계를 정당화한 지점 ⭐

배포 후 스테이징 그룹 2에 집계를 돌리고 SQL과 대조했습니다. **모든 수치가 정확히 일치**했고,
그 과정에서 이것이 드러났습니다.

```
종료된 대화 13건
  ├ 해결      2건  (상담원 종료 1 · 묻고-닫음 1)
  └ 미해결   11건  (묻고-닫음이나 고객이 마지막으로 말함)

"종료 = 해결"로 셌다면  →  13건 · 해결률 100%
실제 규칙으로 세면      →   2건 · 해결률 15%
```

**6.5배 차이입니다.** REQ에서 "방치된 대화가 가장 빠른 해결로 집계된다"고 적은 것이 실데이터로
확인됐습니다. 이 그룹은 고객이 마지막으로 말하고 답을 못 받은 채 닫힌 대화가 대다수인데,
순진한 정의였다면 **해결률 100%짜리 우수 사례**로 보였을 것입니다.

정의 하나가 리포트의 결론을 뒤집습니다. 그래서 그 정의를 리포트 본문에 인쇄합니다.

## 4-1. 생성된 리포트로 확인한 것 (H-4 해소)

tenant 4 · 그룹 4 · `claude-opus-4-8` · 28초 · 4,992자.

| 본문 | `metrics_json` | |
|---|---|---|
| 세션 2 · 대화 2 | 2 · 2 | ✓ |
| 해결 0 · 미해결 2(open) | 0 · 2 · `{open:2}` | ✓ |
| 메시지 7 (고객 2, 상담원 3) | 7 · 2 · 3 | ✓ |
| 왕복 1.5 · 이관 2 | 1.5 · 2 | ✓ |
| 채널 widget 2 · 단계 Awareness 2·Inquiry 2 | 동일 | ✓ |

**금지 규칙이 실제로 작동했습니다** — 이것이 이 검증의 요점입니다.

- **없는 숫자를 지어내지 않았습니다.** 메시지 7 ≠ 고객 2 + 상담원 3(나머지는 시스템)인데,
  모델은 *"The remaining message count breakdown beyond these fields was not measured"*라고
  썼습니다. "시스템 2건"으로 채울 수 있던 자리입니다.
- **0분을 쓰지 않았습니다.** 중앙값이 null이라 *"not measured"*로 적고 이유(해결 0건이라 잴
  구간이 없음)까지 밝혔습니다. 0분이었다면 **즉시 해결**로 읽혔을 것입니다.
- **Aware/Appeal을 비웠습니다** — *"Not observable from support logs"*. Advocate는 구매가
  아니라 **옹호 발화 부재**를 근거로 "없음" 판정.
- **매슬로우는 인용 → 가설 → 반증조건** 3종을 지켰습니다.

## 5. 잔여

| # | 내용 |
|---|---|
| ~~R-1~~ | ~~end-to-end 미검증~~ → **해소(2026-08-25)**: 실제 리포트가 생성됐고 본문 숫자를 `metrics_json`과 전수 대조했습니다. §4-1 참조 |
| R-2 | 콘솔 실화면 스모크 TCR §3 H-1~H-12 |
| R-3 | `primaryChannel`이 동수일 때 임의로 갈립니다(sms 2 / kakao 2 실측). 본문이 "주 채널"을 단정하지 않도록 확인 |
| R-4 | vi/ja/zh 신규 문구는 LLM 초벌(β) |
| R-5 | 리포트 비용은 `feature='journey_report'`로 계측됩니다([[ai-usage-metering]]). 첫 실사용 후 1건당 토큰을 보고 `sample_cap` 기본값(200) 재검토 |
| R-6 | 프로덕션 배포 시 SQL 선적용 |
