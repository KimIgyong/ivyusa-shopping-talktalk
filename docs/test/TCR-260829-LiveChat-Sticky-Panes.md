# TCR-260829 라이브챗 스크롤 분리 — 테스트 케이스

- 근거: `docs/plan/PLN-260829-LiveChat-Sticky-Panes.md` (승인: 구현 진행)
- 성격: 웹 레이아웃 전용 — 유닛 대상 로직 없음. 게이트=typecheck/build, 검증=스테이징 실측(육안).

## 1. 빌드 게이트
- `typecheck`(web)·`build`(web) ✅ (2026-08-29)

## 2. 스테이징 실측 (배포 후)

| # | 시나리오 | 기대 |
|---|---|---|
| S1 | ivyusa 세션 50건 목록 최하단까지 스크롤 | 제목줄·상태 탭·그룹/채널/에이전트 필터·검색바 상시 노출(sticky), 목록 행이 그 아래로 지나감(불투명 배경) |
| S2 | 스크롤 중 필터 조작 | 탭 전환·채널 셀렉트·검색 입력이 즉시 동작, 목록 1페이지 갱신 |
| S3 | 긴 대화(100+ 메시지) 과거 로드 후 위아래 스크롤 | 상단 세션명/상태/배지 줄 + 버튼줄(지정·이슈·싱크·수락·종료) + IssuePanel + 컴포저 고정, **메시지만 스크롤** |
| S4 | 본문(body) 스크롤 부재 | 창 높이 700px/1080px에서 페이지 스크롤바 없음 — 스크롤은 세 내부 영역뿐 |
| S5 | 우측 컨텍스트 칼럼 | 카드가 많아도 칼럼 내부 스크롤, 페이지 무영향 |
| S6 | 그룹 탭·그룹 룸 | 목록 sticky 동일 적용, 그룹 룸 무회귀 |
| S7 | 조건부 상단 배너(비밀번호 변경 안내) 세션 | 본문 스크롤 대신 그리드 축소로 흡수(D2 설계 동작) |

결과는 RPT-260829에 기록.
