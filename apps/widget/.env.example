VITE_API_BASE_URL=http://localhost:3000/api/v1

# GA4 analytics (widget). Unset = analytics disabled (no-op). Per-store override
# via IVY_WIDGET_CONFIG.ga4Id in embed.js. Consent-gated (GA4 Consent Mode v2).
VITE_GA4_MEASUREMENT_ID=
